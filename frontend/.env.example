# API Configuration
# Base URL for API requests (must include /api path for production with Traefik)
REACT_APP_API_URL=http://localhost/api

# WebSocket URL for real-time chat
# Should be the domain without /api path, as the chatService will append /api/chat/ws/...
# Can be either:
#   - Full URL with protocol: ws://localhost or wss://t.lrnm.eu
#   - Just hostname: localhost (protocol will be auto-detected)
REACT_APP_WS_URL=ws://localhost

# Development Configuration
REACT_APP_ENVIRONMENT=development
REACT_APP_DEBUG=true

# Feature Flags
REACT_APP_ENABLE_ANALYTICS=false
REACT_APP_ENABLE_OFFLINE_MODE=false